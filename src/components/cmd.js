'use strict';

import React from 'react';
import { render } from 'react-dom';


const Cmd = () => {
  const style = {
    border: 'solid 1px #FFFFFF',
    background: '#333333',
    color: '#FFFFFF'
  };
  return (
    <div style={style}>
      Cmd
    </div>
  );
}

export default Cmd;
